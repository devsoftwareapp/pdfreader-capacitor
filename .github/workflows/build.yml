name: Hello Capacitor Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout (boş repo bile olsa gerekli)
        uses: actions/checkout@v3

      - name: Node 20 kur
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      - name: Java 17 kur (Android için)
        uses: actions/setup-java@v3
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Android SDK kur
        uses: android-actions/setup-android@v3

      - name: Projeyi sıfırdan oluştur
        run: |
          mkdir hello-capacitor
          cd hello-capacitor
          
          npm init -y
          npm install @capacitor/core @capacitor/cli

          # capacitor.config.json oluştur
          cat << 'EOF' > capacitor.config.json
          {
            "appId": "com.example.hello",
            "appName": "Hello Capacitor",
            "webDir": "www",
            "bundledWebRuntime": false
          }
          EOF

          # www klasörünü ve index.html dosyasını oluştur
          mkdir www
          cat << 'EOF' > www/index.html
          <!DOCTYPE html>
          <html>
            <head>
              <meta charset="utf-8" />
              <title>Hello Capacitor</title>
            </head>
            <body>
              <h1>Hello Capacitor!</h1>
            </body>
          </html>
          EOF

      - name: Android ekle & build
        working-directory: hello-capacitor
        run: |
          npx cap add android
          npx cap sync android
          cd android
          ./gradlew assembleDebug

      - name: APK'yi artifact olarak yükle
        uses: actions/upload-artifact@v3
        with:
          name: hello-capacitor-apk
          path: hello-capacitor/android/app/build/outputs/apk/debug/*.apk
